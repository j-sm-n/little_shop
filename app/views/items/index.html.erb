<h1>Items</h1>

<% @items.each do |item| %>
  <p>
    <%= image_tag item.image_path -%>
  </p>

  <p>
    <%= item.title %>
  </p>

  <p>
    <%= item.description %>
  </p>

  <p>
    <%= item.price %>
  </p>
<% end %>

<%= button_to "Add to Cart", cart_items_path %>
